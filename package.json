{
  "name": "media-player",
  "version": "1.0.0",
  "description": "media-player",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder"
  },
  "build": {
    "appId": "com.example.media-player",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowToChangeInstallationDirectory": true
    }
  },
  "author": "parthi",
  "license": "MIT",
  "dependencies": {
    "howler": "^2.2.4"
  },
  "devDependencies": {
    "electron-builder": "^24.9.1",
    "@electron/remote": "^2.1.0",
    "electron": "^27.1.3",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
